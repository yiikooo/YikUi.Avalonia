<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="YikUi.Demo.Pages.DialogPage"
    x:DataType="vm:DialogDemoViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:YikUi.Demo.Pages"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:yik="https://github.com/yiikooo/YikUi.Avalonia">
    <yik:GroupBorder
        Header="Dialog"
        Margin="15"
        Padding="15">
        <Grid ColumnDefinitions="Auto, *">
            <ScrollViewer>
                <TabControl Grid.Column="0" TabStripPlacement="Left">
                    <TabItem Header="Default Window Dialog">
                        <yik:Form
                            DataContext="{Binding DefaultWindowDialogDemoViewModel}"
                            LabelPosition="Top"
                            Margin="16,0"
                            Width="300">
                            <TextBox
                                HorizontalAlignment="Stretch"
                                Text="{Binding Title}"
                                yik:FormItem.Label="Title" />
                            <yik:EnumSelector
                                EnumType="WindowStartupLocation"
                                HorizontalAlignment="Stretch"
                                Value="{Binding Location}"
                                yik:FormItem.Label="Startup Location" />
                            <yik:FormItem Label="PixelPosition">
                                <UniformGrid Columns="2" Rows="1">
                                    <yik:NumericIntUpDown InnerLeftContent="X" Value="{Binding X}" />
                                    <yik:NumericIntUpDown InnerLeftContent="Y" Value="{Binding Y}" />
                                </UniformGrid>
                            </yik:FormItem>
                            <yik:EnumSelector
                                EnumType="yik:DialogMode"
                                HorizontalAlignment="Stretch"
                                Value="{Binding Mode}"
                                yik:FormItem.Label="Dialog Mode" />
                            <yik:EnumSelector
                                EnumType="yik:DialogButton"
                                HorizontalAlignment="Stretch"
                                Value="{Binding Button}"
                                yik:FormItem.Label="Dialog Buttons" />
                            <CheckBox IsChecked="{Binding ShowInTaskBar}" yik:FormItem.Label="Show In Taskbar" />
                            <CheckBox
                                IsChecked="{Binding IsCloseButtonVisible}"
                                IsThreeState="True"
                                yik:FormItem.Label="Is Close Button Visible" />
                            <CheckBox IsChecked="{Binding CanDragMove}" yik:FormItem.Label="CanDragMove" />
                            <CheckBox IsChecked="{Binding CanResize}" yik:FormItem.Label="CanResize" />
                            <yik:FormItem>
                                <yik:FormItem.Label>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Style Class" />
                                        <PathIcon
                                            Data="{DynamicResource SemiIconHelpCircle}"
                                            TextElement.FontWeight="Normal"
                                            Theme="{StaticResource InnerPathIcon}"
                                            ToolTip.Tip="Pass a Style Class to the created Dialog. In this example, if you set StyleClass as 'Custom', You will get Special Style for OK Button. These styles usually are defined in the root of your App/Window"
                                            VerticalAlignment="Center" />
                                    </StackPanel>
                                </yik:FormItem.Label>
                                <TextBox HorizontalAlignment="Stretch" Text="{Binding StyleClass}" />
                            </yik:FormItem>
                            <Button
                                Command="{Binding ShowDialogCommand}"
                                Content="Show"
                                HorizontalAlignment="Left"
                                yik:FormItem.NoLabel="True" />
                        </yik:Form>
                    </TabItem>
                    <TabItem Header="Custom Window Dialog">
                        <yik:Form
                            DataContext="{Binding CustomWindowDialogDemoViewModel}"
                            LabelPosition="Top"
                            Margin="16,0"
                            Width="300">
                            <TextBox
                                HorizontalAlignment="Stretch"
                                Text="{Binding Title}"
                                yik:FormItem.Label="Title" />
                            <yik:EnumSelector
                                EnumType="WindowStartupLocation"
                                HorizontalAlignment="Stretch"
                                Value="{Binding Location}"
                                yik:FormItem.Label="Startup Location" />
                            <yik:FormItem Label="PixelPosition">
                                <UniformGrid Columns="2" Rows="1">
                                    <yik:NumericIntUpDown InnerLeftContent="X" Value="{Binding X}" />
                                    <yik:NumericIntUpDown InnerLeftContent="Y" Value="{Binding Y}" />
                                </UniformGrid>
                            </yik:FormItem>
                            <CheckBox IsChecked="{Binding ShowInTaskBar}" yik:FormItem.Label="Show In Taskbar" />
                            <CheckBox
                                IsChecked="{Binding IsCloseButtonVisible}"
                                IsThreeState="True"
                                yik:FormItem.Label="Is Close Button Visible" />
                            <CheckBox IsChecked="{Binding IsModal}" yik:FormItem.Label="Modal" />
                            <CheckBox IsChecked="{Binding CanDragMove}" yik:FormItem.Label="CanDragMove" />
                            <CheckBox IsChecked="{Binding CanResize}" yik:FormItem.Label="CanResize" />
                            <Button
                                Command="{Binding ShowDialogCommand}"
                                Content="Show"
                                HorizontalAlignment="Left"
                                yik:FormItem.NoLabel="True" />
                        </yik:Form>
                    </TabItem>
                    <TabItem Header="Default Overlay Dialog">
                        <yik:Form
                            DataContext="{Binding DefaultOverlayDialogDemoViewModel}"
                            LabelPosition="Top"
                            Margin="16,0"
                            Width="300">
                            <TextBox
                                HorizontalAlignment="Stretch"
                                Text="{Binding Title}"
                                yik:FormItem.Label="Title" />
                            <yik:EnumSelector
                                EnumType="yik:DialogMode"
                                HorizontalAlignment="Stretch"
                                Value="{Binding Mode}"
                                yik:FormItem.Label="Dialog Mode" />
                            <yik:EnumSelector
                                EnumType="yik:DialogButton"
                                HorizontalAlignment="Stretch"
                                Value="{Binding Button}"
                                yik:FormItem.Label="Dialog Buttons" />
                            <CheckBox
                                IsChecked="{Binding IsCloseButtonVisible}"
                                IsThreeState="True"
                                yik:FormItem.Label="Is Close Button Visible" />
                            <CheckBox IsChecked="{Binding IsModal}" yik:FormItem.Label="Modal" />
                            <CheckBox IsChecked="{Binding CanDragMove}" yik:FormItem.Label="Can DragMove" />
                            <CheckBox IsChecked="{Binding CanLightDismiss}" yik:FormItem.Label="Can LightDismiss" />
                            <CheckBox IsChecked="{Binding FullScreen}" yik:FormItem.Label="FullScreen" />
                            <CheckBox IsChecked="{Binding CanResize}" yik:FormItem.Label="CanResize" />
                            <ToggleSwitch
                                IsChecked="{Binding IsLocal}"
                                OffContent="Global"
                                OnContent="Local"
                                yik:FormItem.Label="Global/Local OverlayHost" />
                            <yik:FormItem>
                                <yik:FormItem.Label>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Style Class" />
                                        <PathIcon
                                            Data="{DynamicResource SemiIconHelpCircle}"
                                            TextElement.FontWeight="Normal"
                                            Theme="{StaticResource InnerPathIcon}"
                                            ToolTip.Tip="Pass a Style Class to the created Dialog. In this example, if you set StyleClass as 'Custom', You will get Special Style for OK Button. These styles usually are defined in the root of your App/Window"
                                            VerticalAlignment="Center" />
                                    </StackPanel>
                                </yik:FormItem.Label>
                                <TextBox HorizontalAlignment="Stretch" Text="{Binding StyleClass}" />
                            </yik:FormItem>
                            <Button
                                Command="{Binding ShowDialogCommand}"
                                Content="Show"
                                HorizontalAlignment="Left"
                                yik:FormItem.NoLabel="True" />
                        </yik:Form>
                    </TabItem>
                    <TabItem Header="Custom Overlay Dialog">
                        <yik:Form
                            DataContext="{Binding CustomOverlayDialogDemoViewModel}"
                            LabelPosition="Top"
                            Margin="16,0"
                            Width="300">
                            <TextBox
                                HorizontalAlignment="Stretch"
                                Text="{Binding Title}"
                                yik:FormItem.Label="Title" />
                            <CheckBox
                                IsChecked="{Binding IsCloseButtonVisible}"
                                IsThreeState="True"
                                yik:FormItem.Label="Is Close Button Visible" />
                            <CheckBox IsChecked="{Binding IsModal}" yik:FormItem.Label="Modal" />
                            <CheckBox IsChecked="{Binding CanDragMove}" yik:FormItem.Label="Can DragMove" />
                            <CheckBox IsChecked="{Binding CanLightDismiss}" yik:FormItem.Label="Can LightDismiss" />
                            <CheckBox IsChecked="{Binding FullScreen}" yik:FormItem.Label="FullScreen" />
                            <CheckBox IsChecked="{Binding CanResize}" yik:FormItem.Label="CanResize" />
                            <ToggleSwitch
                                IsChecked="{Binding IsLocal}"
                                OffContent="Global"
                                OnContent="Local"
                                yik:FormItem.Label="Global/Local OverlayHost" />
                            <Button
                                Command="{Binding ShowDialogCommand}"
                                Content="Show"
                                HorizontalAlignment="Left"
                                yik:FormItem.NoLabel="True" />
                        </yik:Form>
                    </TabItem>
                </TabControl>
            </ScrollViewer>
            <Grid Grid.Column="1">
                <Border
                    Background="{DynamicResource SemiColorBackground1}"
                    BorderBrush="{DynamicResource SemiGrey1}"
                    BorderThickness="1"
                    ClipToBounds="True"
                    CornerRadius="12">
                    <yik:OverlayDialogHost HostId="{x:Static vm:DialogDemoViewModel.LocalHost}">
                        <yik:OverlayDialogHost.DialogDataTemplates>
                            <DataTemplate DataType="x:String">
                                <TextBlock
                                    Foreground="Red"
                                    Margin="24,24,48,24"
                                    Text="{Binding Path=.}" />
                            </DataTemplate>
                        </yik:OverlayDialogHost.DialogDataTemplates>
                    </yik:OverlayDialogHost>
                </Border>
            </Grid>
        </Grid>
    </yik:GroupBorder>
</UserControl>