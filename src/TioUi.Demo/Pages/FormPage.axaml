<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="TioUi.Demo.Pages.FormPage"
    x:DataType="pages:FormDemoViewModel"
    xmlns="https://github.com/avaloniaui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:TioUi.Demo.Pages"
    xmlns:tio="https://github.com/yiikooo/TioUi.Avalonia"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <tio:GroupBorder
        Header="Form"
        Margin="15"
        Padding="15">
        <ScrollViewer>
            <StackPanel HorizontalAlignment="Left" Margin="24,0,0,0">
                <tio:Divider Content="Simple Form" />
                <tio:Form LabelPosition="Left" LabelWidth="*">
                    <TextBox
                        Width="300"
                        tio:FormItem.IsRequired="True"
                        tio:FormItem.Label="Name" />
                    <TextBox Width="300" tio:FormItem.Label="Email" />
                    <TextBox
                        Classes="TextArea"
                        Width="300"
                        tio:FormItem.Label="Message" />
                </tio:Form>
                <tio:Divider Content="Form accessibility" />
                <tio:Form LabelPosition="Left" LabelWidth="*">
                    <TextBox Width="300" tio:FormItem.Label="_Name" />
                    <TextBox Width="300" tio:FormItem.Label="_Email" />
                    <TextBox
                        Classes="TextArea"
                        Width="300"
                        tio:FormItem.Label="_Message" />
                </tio:Form>
                <tio:Divider Content="Label on top" />
                <tio:Form LabelPosition="Top" LabelWidth="*">
                    <TextBox Width="300" tio:FormItem.Label="Name" />
                    <TextBox Width="300" tio:FormItem.Label="Email" />
                </tio:Form>
                <tio:Divider Content="FormGroup" />
                <tio:Form LabelPosition="Left" LabelWidth="*">
                    <tio:FormGroup Header="Basic Information">
                        <TextBox Width="300" tio:FormItem.Label="Name" />
                        <TextBox Width="300" tio:FormItem.Label="Email" />
                    </tio:FormGroup>
                    <tio:FormGroup Header="Education Information">
                        <TextBox Width="300" tio:FormItem.Label="Collage" />
                        <tio:FormItem Label="Study Time">
                            <tio:DateRangePicker Width="300" />
                        </tio:FormItem>
                    </tio:FormGroup>
                    <Button
                        Content="Submit"
                        HorizontalAlignment="Stretch"
                        Theme="{DynamicResource SolidButton}"
                        tio:FormItem.NoLabel="True" />
                </tio:Form>
                <tio:Divider Content="MVpages setup and validation" />
                <tio:Form
                    DataContext="{Binding Model}"
                    LabelPosition="Left"
                    LabelWidth="*">
                    <TextBox
                        Text="{Binding Name}"
                        Width="300"
                        tio:FormItem.Label="Name" />
                    <TextBox
                        Text="{Binding Email}"
                        Width="300"
                        tio:FormItem.Label="Email" />
                </tio:Form>
                <tio:Divider Content="MVpages setup for dynamic items. " />
                <tio:Form
                    HorizontalAlignment="Stretch"
                    ItemsSource="{Binding FormGroups}"
                    LabelPosition="Left"
                    LabelWidth="*">
                    <tio:Form.Styles>
                        <Style Selector="tio|FormGroup" x:DataType="pages:IFormGroupViewModel">
                            <Setter Property="Header" Value="{Binding Title}" />
                            <Setter Property="ItemsSource" Value="{Binding Items}" />
                        </Style>
                        <Style Selector="tio|FormItem" x:DataType="pages:IFromItemViewModel">
                            <Setter Property="Label" Value="{Binding Label}" />
                        </Style>
                    </tio:Form.Styles>
                    <tio:Form.ItemTemplate>
                        <pages:FormDataTemplateSelector>
                            <DataTemplate DataType="pages:FormTextViewModel" x:Key="{x:Type pages:FormTextViewModel}">
                                <TextBox Text="{Binding Value}" />
                            </DataTemplate>
                            <DataTemplate DataType="pages:FormAgeViewModel" x:Key="{x:Type pages:FormAgeViewModel}">
                                <tio:NumericUIntUpDown Value="{Binding Age}" />
                            </DataTemplate>
                            <DataTemplate DataType="pages:FormDateRangeViewModel" x:Key="{x:Type pages:FormDateRangeViewModel}">
                                <tio:DateRangePicker SelectedEndDate="{Binding End}" SelectedStartDate="{Binding Start}" />
                            </DataTemplate>
                        </pages:FormDataTemplateSelector>
                    </tio:Form.ItemTemplate>
                </tio:Form>
            </StackPanel>
        </ScrollViewer>
    </tio:GroupBorder>
</UserControl>