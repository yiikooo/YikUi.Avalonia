<ResourceDictionary
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:yik="https://github.com/yiikooo/YikUi.Avalonia">
    <ControlTheme TargetType="yik:RangeSlider" x:Key="{x:Type yik:RangeSlider}">
        <Setter Property="Background" Value="{DynamicResource SliderTrackBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource SliderTrackForeground}" />
        <Setter Property="TrackWidth" Value="4" />
        <Style Selector="^:horizontal">
            <Setter Property="Template">
                <ControlTemplate TargetType="yik:RangeSlider">
                    <DataValidationErrors>
                        <Grid
                            Background="Transparent"
                            Margin="{TemplateBinding Padding}"
                            RowDefinitions="Auto,Auto,Auto"
                            x:Name="SliderContainer">
                            <Grid.Styles>
                                <Style Selector="TickBar">
                                    <Setter Property="ReservedSpace"
                                            Value="{ReflectionBinding #PART_Track.LowerThumb.Bounds}" />
                                </Style>
                            </Grid.Styles>
                            <TickBar
                                Fill="{DynamicResource SliderTickForeground}"
                                Grid.Row="0"
                                Height="{DynamicResource SliderTickHorizontalHeight}"
                                IsVisible="False"
                                Margin="0,0,0,4"
                                Maximum="{TemplateBinding yik:RangeSlider.Maximum}"
                                Minimum="{TemplateBinding yik:RangeSlider.Minimum}"
                                Name="TopTickBar"
                                Orientation="{TemplateBinding yik:RangeSlider.Orientation}"
                                Placement="Top"
                                TickFrequency="{TemplateBinding yik:RangeSlider.TickFrequency}"
                                Ticks="{TemplateBinding Ticks}"
                                VerticalAlignment="Bottom" />
                            <TickBar
                                Fill="{DynamicResource SliderTickForeground}"
                                Grid.Row="2"
                                Height="{DynamicResource SliderTickHorizontalHeight}"
                                IsVisible="False"
                                Margin="0,4,0,0"
                                Maximum="{TemplateBinding yik:RangeSlider.Maximum}"
                                Minimum="{TemplateBinding yik:RangeSlider.Minimum}"
                                Name="BottomTickBar"
                                Orientation="{TemplateBinding yik:RangeSlider.Orientation}"
                                Placement="Bottom"
                                TickFrequency="{TemplateBinding yik:RangeSlider.TickFrequency}"
                                Ticks="{TemplateBinding Ticks}"
                                VerticalAlignment="Top" />
                            <yik:RangeTrack
                                Cursor="Hand"
                                Grid.Row="1"
                                HorizontalAlignment="Stretch"
                                LowerValue="{Binding LowerValue, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                Maximum="{Binding Maximum, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                Minimum="{Binding Minimum, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                Name="PART_Track"
                                Orientation="{Binding Orientation, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                UpperValue="{Binding UpperValue, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                VerticalAlignment="Stretch">
                                <yik:RangeTrack.LowerSection>
                                    <Border
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="100"
                                        Height="{TemplateBinding TrackWidth}"
                                        Name="PART_LowerSection" />
                                </yik:RangeTrack.LowerSection>
                                <yik:RangeTrack.LowerThumb>
                                    <Thumb
                                        Height="16"
                                        Theme="{DynamicResource SliderThumbTheme}"
                                        Width="16" />
                                </yik:RangeTrack.LowerThumb>
                                <yik:RangeTrack.InnerSection>
                                    <Border
                                        Background="{TemplateBinding Foreground}"
                                        CornerRadius="100"
                                        Height="{TemplateBinding TrackWidth}"
                                        Name="PART_InnerSection" />
                                </yik:RangeTrack.InnerSection>
                                <yik:RangeTrack.UpperThumb>
                                    <Thumb
                                        Height="16"
                                        Theme="{DynamicResource SliderThumbTheme}"
                                        Width="16" />
                                </yik:RangeTrack.UpperThumb>
                                <yik:RangeTrack.UpperSection>
                                    <Border
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="100"
                                        Height="{TemplateBinding TrackWidth}"
                                        Name="PART_UpperSection" />
                                </yik:RangeTrack.UpperSection>
                            </yik:RangeTrack>
                        </Grid>
                    </DataValidationErrors>
                </ControlTemplate>
            </Setter>
        </Style>
        <Style Selector="^:vertical">
            <Setter Property="Template">
                <ControlTemplate TargetType="yik:RangeSlider">
                    <DataValidationErrors>
                        <Grid
                            Background="{DynamicResource SliderContainerBackground}"
                            ColumnDefinitions="Auto,Auto,Auto"
                            Margin="{TemplateBinding Padding}"
                            x:Name="SliderContainer">
                            <Grid.Styles>
                                <Style Selector="TickBar">
                                    <Setter Property="ReservedSpace"
                                            Value="{ReflectionBinding #PART_Track.LowerThumb.Bounds}" />
                                </Style>
                            </Grid.Styles>
                            <TickBar
                                Fill="{DynamicResource SliderTickForeground}"
                                Grid.Column="0"
                                HorizontalAlignment="Right"
                                IsVisible="False"
                                Margin="0,0,4,0"
                                Maximum="{TemplateBinding yik:RangeSlider.Maximum}"
                                Minimum="{TemplateBinding yik:RangeSlider.Minimum}"
                                Name="LeftTickBar"
                                Orientation="{TemplateBinding yik:RangeSlider.Orientation}"
                                Placement="Left"
                                TickFrequency="{TemplateBinding yik:RangeSlider.TickFrequency}"
                                Ticks="{TemplateBinding Ticks}"
                                Width="{DynamicResource SliderTickVerticalWidth}" />
                            <TickBar
                                Fill="{DynamicResource SliderTickForeground}"
                                Grid.Column="2"
                                HorizontalAlignment="Left"
                                IsVisible="False"
                                Margin="4,0,0,0"
                                Maximum="{TemplateBinding yik:RangeSlider.Maximum}"
                                Minimum="{TemplateBinding yik:RangeSlider.Minimum}"
                                Name="RightTickBar"
                                Orientation="{TemplateBinding yik:RangeSlider.Orientation}"
                                Placement="Right"
                                TickFrequency="{TemplateBinding yik:RangeSlider.TickFrequency}"
                                Ticks="{TemplateBinding Ticks}"
                                Width="{DynamicResource SliderTickVerticalWidth}" />
                            <yik:RangeTrack
                                Cursor="Hand"
                                Grid.Column="1"
                                HorizontalAlignment="Stretch"
                                LowerValue="{Binding LowerValue, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                Maximum="{Binding Maximum, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                Minimum="{Binding Minimum, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                Name="PART_Track"
                                Orientation="{Binding Orientation, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                UpperValue="{Binding UpperValue, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                VerticalAlignment="Stretch">
                                <yik:RangeTrack.LowerSection>
                                    <Border
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="100"
                                        Name="PART_LowerSection"
                                        Width="{TemplateBinding TrackWidth}" />
                                </yik:RangeTrack.LowerSection>
                                <yik:RangeTrack.LowerThumb>
                                    <Thumb
                                        Height="16"
                                        Theme="{DynamicResource SliderThumbTheme}"
                                        Width="16" />
                                </yik:RangeTrack.LowerThumb>
                                <yik:RangeTrack.InnerSection>
                                    <Border
                                        Background="{TemplateBinding Foreground}"
                                        CornerRadius="100"
                                        Name="PART_InnerSection"
                                        Width="{TemplateBinding TrackWidth}" />
                                </yik:RangeTrack.InnerSection>
                                <yik:RangeTrack.UpperThumb>
                                    <Thumb
                                        Height="16"
                                        Theme="{DynamicResource SliderThumbTheme}"
                                        Width="16" />
                                </yik:RangeTrack.UpperThumb>
                                <yik:RangeTrack.UpperSection>
                                    <Border
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="100"
                                        Name="PART_UpperSection"
                                        Width="{TemplateBinding TrackWidth}" />
                                </yik:RangeTrack.UpperSection>
                            </yik:RangeTrack>
                        </Grid>
                    </DataValidationErrors>
                </ControlTemplate>
            </Setter>
        </Style>
        <Style
            Selector="^[TickPlacement=TopLeft] /template/ TickBar#LeftTickBar, ^[TickPlacement=Outside] /template/ TickBar#LeftTickBar">
            <Setter Property="IsVisible" Value="True" />
        </Style>

        <Style
            Selector="^[TickPlacement=TopLeft] /template/ TickBar#TopTickBar, ^[TickPlacement=Outside] /template/ TickBar#TopTickBar">
            <Setter Property="IsVisible" Value="True" />
        </Style>

        <Style
            Selector="^[TickPlacement=BottomRight] /template/ TickBar#BottomTickBar, ^[TickPlacement=Outside] /template/ TickBar#BottomTickBar">
            <Setter Property="IsVisible" Value="True" />
        </Style>

        <Style
            Selector="^[TickPlacement=BottomRight] /template/ TickBar#RightTickBar, ^[TickPlacement=Outside] /template/ TickBar#RightTickBar">
            <Setter Property="IsVisible" Value="True" />
        </Style>

        <Style Selector="^:error /template/ Thumb#thumb">
            <Setter Property="BorderBrush" Value="{DynamicResource DataValidationErrorsSelectedBorderBrush}" />
        </Style>

        <Style Selector="^:disabled">
            <Style Selector="^ /template/ Border#PART_InnerSection">
                <Setter Property="Background" Value="{DynamicResource SliderTrackDisabledForeground}" />
            </Style>

            <Style Selector="^ /template/ Border#PART_UpperSection, ^ /template/ Border#PART_LowerSection">
                <Setter Property="Background" Value="{DynamicResource SliderTrackDisabledBackground}" />
            </Style>

            <Style Selector="^ /template/ Thumb">
                <Setter Property="BorderBrush" Value="{DynamicResource SliderThumbDisabledBorderBrush}" />
            </Style>
        </Style>
    </ControlTheme>
</ResourceDictionary>